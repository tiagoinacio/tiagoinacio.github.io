{
  "name": "tiagoinacio",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf dist/ && mkdir dist/ && cp -Rf images dist/images && cp style.css dist/ && node scripts/minify-html.js"
  },
  "repository": {
    "type": "git",
    "url": "git@github.io:tiagoinacio/tiagoinacio.github.io.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "html-minifier": "^3.2.3"
  }
}
